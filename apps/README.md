This folder contains NoxMQTT applications built on top of the reusable client library.

The primary host-side application is `apps/noxmqtt`, a cross-platform CLI that supports:

- one-shot `pub`, `sub`, and `unsub` commands
- interactive shell mode
- TCP and NoxTLS-backed secure connections through the transport modules
