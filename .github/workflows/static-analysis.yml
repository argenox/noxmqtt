name: Static Analysis

on:
  pull_request:
  push:

permissions:
  contents: read

jobs:
  cppcheck:
    name: Cppcheck
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install cppcheck
        run: |
          sudo apt-get update
          sudo apt-get install -y cppcheck

      - name: Run cppcheck
        run: |
          mkdir -p reports
          cppcheck_exit=0
          cppcheck \
            --file-list=.github/cppcheck-files.txt \
            --enable=warning,style,performance,portability,information,missingInclude \
            --inconclusive \
            --force \
            --inline-suppr \
            --quiet \
            --std=c11 \
            --error-exitcode=1 \
            --suppress=missingIncludeSystem \
            --suppress=unusedFunction \
            -Isrc/noxmqtt-lib \
            -Iapps/noxmqtt \
            --xml \
            --xml-version=2 \
            2> reports/cppcheck.xml || cppcheck_exit=$?

          if [ "$cppcheck_exit" -ne 0 ]; then
            echo "cppcheck reported findings; summary follows:"
            cat reports/cppcheck.xml
            exit "$cppcheck_exit"
          fi

      - name: Upload cppcheck report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cppcheck-report
          path: reports/cppcheck.xml
